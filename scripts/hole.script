function on_message(self, message_id, message, sender)
    if message_id == hash("trigger_response") and message.enter and message.other_group == hash("ball") then
        msg.post("/controller#script", "level_complete")
    end
end
